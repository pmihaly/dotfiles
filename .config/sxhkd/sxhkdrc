super + w
    brave

super + x
    slock

super + shift + x
    poweroff

super + a
    $TERMINAL -e pulsemixer

super + o
    zathura ~/sync/orarend.pdf

super + r
    $TERMINAL $FILEBROWSER

super + c
    $TERMINAL -e python -ic ""

super + e
    $TERMINAL -e neomutt

F11
    scrot

super + y
    $TERMINAL -e bash -ic 'yt -s'

super + v
    $TERMINAL -e nvim -c VimwikiIndex
