# gui programs
super + {w, i, C}
    {firefox, dmenuunicode, dmenuqalc}

# terminal programs
super + {a, e, r, v, p}
    $TERMINAL -e {pulsemixer, neomutt, $FILEBROWSER, $EDITOR -c VimwikiIndex, projectfzf}

# content consooming
super + {n, y, J, S}
    $TERMINAL -e {newsboat -c ~/sync/newsboat-cache, ytfzf -lt, jftui, streamfzf}

# dotfiles obsession
super + {c, s}
    $TERMINAL -e {configfzf, scriptfzf}

# system programs
super + {x, X}
    {slock, poweroff}

Print
    scrot
