# gui programs
super + {w, i, C, t}
    {firefox, dmenuunicode, dmenuqalc, dmenutrans}

# terminal programs
super + {a, r, v, o, g}
    $TERMINAL -e {pulsemixer, $FILEBROWSER, $EDITOR -c VimwikiIndex, projectfzf, tuxifzf}

super + e
    mw -Y & $TERMINAL -e neomutt

# content consooming
super + {n, y, J, S}
    $TERMINAL -e {newsboat -c ~/sync/newsboat-cache, ytfzf -lt, jftui, streamfzf}

# dotfiles obsession
super + {c, s}
    $TERMINAL -e {configfzf, scriptfzf}

# system
super + {W, x, X}
    {dmenuwifi, slock, poweroff}

# screenshot
super + {p, P}
    mkdir -p "$XDG_PICTURES_DIR/screens" ; ({maim, maim -s} | tee "$XDG_PICTURES_DIR/screens/$(date +%s).png" | xclip -selection clipboard -t image/png) && notify-send "📸 Képernyő lescreenelve"
