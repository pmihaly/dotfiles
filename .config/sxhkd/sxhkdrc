super + w
    $BROWSER

super + x
    slock

super + shift + x
    poweroff

super + a
    $TERMINAL -e pulsemixer

super + o
    zathura ~/sync/orarend.pdf

super + r
    $TERMINAL -e $FILEBROWSER

super + e
    $TERMINAL -e neomutt

F11
    scrot

super + y
    $TERMINAL -e ytfzf -tls

super + v
    $TERMINAL -e nvim -c VimwikiIndex

super + i
    dmenuunicode

super + n
    $TERMINAL -e newsboat

super + c
    $TERMINAL -e ~/.local/bin/c
