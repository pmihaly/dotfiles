super + w
    $BROWSER

super + x
    slock

super + shift + x
    poweroff

super + a
    $TERMINAL -e pulsemixer

super + o
    zathura ~/sync/orarend.pdf

super + r
    $TERMINAL -e $FILEBROWSER

super + c
    $TERMINAL -e python -ic ""

super + e
    $TERMINAL -e neomutt

F11
    scrot

super + y
    $TERMINAL -e $SHELL -ic 'yt -s'

super + v
    $TERMINAL -e nvim -c VimwikiIndex

super + i
    dmenuunicode
