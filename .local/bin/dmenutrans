#!/bin/sh

# Translates english and german to system default language
# Depends on: dmenu, translate-shell

input="$(dmenu -p "trans: " </dev/null)"

trans -b -s "en+de" "$input" | sed "/$input/d" | uniq | dmenu >/dev/null
